We need to debug and fix two UI issues in the D-Bolt-AI React + Vite frontend.

CONTEXT:
- Phase 3 implementation completed successfully.
- Zustand persist middleware works.
- Routing works (/ and /chat).
- AppLayout is used globally.
- Running inside Replit dev environment.
- DO NOT change architecture or remove existing features.
- Apply minimal safe fixes only.

--------------------------------
ISSUE 1 — Chat History Not Visible
--------------------------------

Expected behavior:
- Chat sessions stored in Zustand should appear in a sidebar or menu.
- User must be able to see and switch chat history sessions.

Tasks:

1. Inspect:
   - AppLayout.tsx
   - ChatLayout / Sidebar components
   - store/useChatStore.ts (or equivalent Zustand store)

2. Verify:
   - sessions state exists
   - sessions.length is mapped to UI
   - sidebar/menu component is mounted inside /chat route

3. Fix if needed:
   - Ensure Sidebar component renders inside Chat page layout
   - Add conditional rendering:

     {sessions.length > 0 && <Sidebar />}

4. Confirm session list maps correctly:

   sessions.map(session => (
     <SessionItem key={session.id} />
   ))

5. Ensure mobile responsiveness does NOT hide sidebar permanently
   (check Tailwind classes like hidden md:flex).

--------------------------------
ISSUE 2 — Landing Page Cannot Scroll
--------------------------------

Problem:
Home page loads but scrolling up/down is locked.

Tasks:

1. Inspect:
   - AppLayout.tsx
   - main container wrappers
   - index.css / global.css
   - Landing.tsx root container

2. Look for problematic styles:
   - overflow: hidden
   - height: 100vh
   - fixed positioning blocking scroll
   - body or html overflow locks

3. Apply fix:

   Ensure:

   html, body, #root {
     height: 100%;
     overflow-x: hidden;
   }

   Main content container should use:
   min-h-screen
   NOT h-screen unless required.

4. Remove any global overflow:hidden unless needed for modals.

--------------------------------
VALIDATION REQUIRED
--------------------------------

After fixes:

✅ Landing page scrolls normally
✅ User can scroll both directions
✅ Chat history sidebar/menu appears
✅ Sessions selectable
✅ No console errors
✅ No layout shift
✅ Mobile layout still responsive

--------------------------------
OUTPUT REQUIRED
--------------------------------

- Show files modified
- Explain root cause briefly
- Confirm fixes applied safely